/**
 * ikigai-deno: a TypeScript (Deno-first) client and servable peer for the
 * ikigai wire protocol over Unix domain sockets. L0 of the polyglot ladder —
 * implementation lands via PR; this is the repo skeleton.
 */

/** The wire protocol version this package will speak. */
export const PROTOCOL_VERSION = 6;
