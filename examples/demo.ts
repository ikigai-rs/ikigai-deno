/** The money demo: TypeScript functions served as ikigai resources.
 * Placeholder until the implementation PR lands. */

import { PROTOCOL_VERSION } from "../src/mod.ts";

if (import.meta.main) {
  console.error(
    `ikigai-deno demo: not implemented yet (wire v${PROTOCOL_VERSION})`,
  );
  Deno.exit(1);
}
